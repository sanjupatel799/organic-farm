{
  "name": "organic-farm",
  "version": "1.0.0",
  "private": true,
  "description": "Organic Farm E-commerce - Full Stack",
  "scripts": {
    "dev": "concurrently --names \"API,WEB\" --prefix-colors \"green,blue\" \"cd backend && npm run start:dev\" \"cd frontend && npm run dev\"",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "db:push": "cd backend && npx prisma db push",
    "db:seed": "cd backend && npx tsx prisma/seed.ts",
    "db:studio": "cd backend && npx prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
